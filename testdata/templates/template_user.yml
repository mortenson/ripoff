rows:
  {{ .rowId }}:
    email: {{ .email }}
    avatar_id: avatars:uuid({{ .rowId }})
  avatars:uuid({{ .rowId }}):
    url: {{ .avatarUrl }}
  avatar_modifiers:uuid({{ .rowId }}):
    grayscale: {{ .avatarGrayscale }}
